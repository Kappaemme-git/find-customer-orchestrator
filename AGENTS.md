# Find Customer Automation

When Francesco asks for prospects, a selected demo, or a four-day follow-up in this project, use the installed `find-customer-orchestrator` skill. Read `WORKFLOW.md` and use `scripts/workflow.py` for the persistent register and Vercel preview lifecycle.

This project runs on the Windows PC with Claude Code and Vercel CLI already authenticated. Computer Use is not part of the workflow. Francesco sends outreach himself. Do not treat a draft as a sent message or remove a preview based on silence.
